/**
 * @todo Consider to change the name of the interface, since it represents the information of an user and register is not descriptive
 */
export interface Register {
  nickName: string;
  fullName: string;
  email: string;
  password: string;
}
